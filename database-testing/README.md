# Database Validation

This is a **representative local PostgreSQL schema** for demonstrating SQL QA skills. It is not DummyJSON's private database.

Run `schema.sql`, then `seed.sql`, then execute `validation_queries.sql` query-by-query and record outcomes in your test documentation.
